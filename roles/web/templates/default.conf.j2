server {
    listen 0.0.0.0:80 default_server proxy_protocol;
    set_real_ip_from {{ hostvars.loadbalancer.ansible_host }}/32;
    real_ip_header proxy_protocol;

    location / {
        root /var/www;
        index index.html;
    }
}
